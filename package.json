{
  "name": "holdfast-safety-pin-platform",
  "version": "1.0.0",
  "license": "MIT",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --filter @workspace/safety-pin-manufacturer run dev",
    "build": "pnpm --filter @workspace/safety-pin-manufacturer run build",
    "serve": "pnpm --filter @workspace/safety-pin-manufacturer run serve",
    "typecheck": "tsc --build && pnpm --filter \"@workspace/*\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
